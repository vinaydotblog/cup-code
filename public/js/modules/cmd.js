// Generated by CoffeeScript 1.6.3
/*
	All Executable Commands
*/


(function() {
  define(['underscore'], function(_) {
    var Commands;
    Commands = (function() {
      var cmds;

      function Commands() {}

      cmds = {
        intro: function(n) {
          return alert("My name is " + n);
        },
        open_docs: function(a, b) {
          return alert("Opening docs for " + a + " and " + b);
        },
        select_next: function() {
          return Editor.$curEditor.selectMore();
        },
        select_all: function() {
          return Editor.$curEditor.selectAll();
        }
      };

      Commands.exec = function(cmd, args) {
        if (!cmds[cmd]) {
          return alert("Invalid Command!");
        }
        return cmds[cmd].apply(null, args);
      };

      Commands.extend = function(obj) {
        return _.extend(cmds, obj);
      };

      Commands.define = function(name, fn) {
        return cmds[name] = fn;
      };

      return Commands;

    })();
    return window.cmd = Commands;
  });

}).call(this);

/*
//@ sourceMappingURL=cmd.map
*/
