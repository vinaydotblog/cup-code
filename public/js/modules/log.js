// Generated by CoffeeScript 1.6.3
(function() {
  define(function() {
    var Logging, nativeConsole;
    nativeConsole = window.console;
    Logging = (function() {
      var autoName, groupName, history;

      history = [];

      autoName = 0;

      groupName = null;

      function Logging() {
        history.push(arguments);
        nativeConsole.log.apply(nativeConsole, arguments);
      }

      Logging.args = function() {
        return log(arguments.callee.caller["arguments"]);
      };

      Logging.history = function() {
        return history;
      };

      Logging.g = function(name) {
        groupName = name || 'group_' + ++autoName;
        return nativeConsole.groupCollapsed(groupName);
      };

      Logging.ge = function(name) {
        return nativeConsole.groupEnd(name || groupName);
      };

      Logging.er = function() {
        return nativeConsole.error.apply(nativeConsole, arguments);
      };

      Logging.t = function() {
        return nativeConsole.table.apply(nativeConsole, arguments);
      };

      return Logging;

    })();
    return window.log = Logging;
  });

}).call(this);

/*
//@ sourceMappingURL=log.map
*/
